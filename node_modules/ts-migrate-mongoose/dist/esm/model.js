import { Schema } from 'mongoose';
export const getMigrationModel = (connection, collection) => {
    const MigrationSchema = new Schema({
        name: String,
        state: {
            type: String,
            enum: ['down', 'up'],
            default: 'down',
        },
        createdAt: {
            type: Date,
            index: true,
        },
        updatedAt: {
            type: Date,
        },
    }, {
        collection,
        autoCreate: true,
        timestamps: true,
    });
    MigrationSchema.virtual('filename').get(function () {
        return `${this.createdAt.getTime().toString()}-${this.name}.ts`;
    });
    return connection.model(collection, MigrationSchema);
};
//# sourceMappingURL=model.js.map