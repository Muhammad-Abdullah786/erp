import Migrator from './migrator';
import type IOptions from './interfaces/IOptions';
export declare const getConfig: (configPath: string) => Promise<IOptions>;
export declare const getMigrator: (options: IOptions) => Promise<Migrator>;
export declare class Migrate {
    private program;
    private migrator;
    constructor();
    finish(exit: boolean, error?: Error): Promise<IOptions>;
    run(exit?: boolean): Promise<IOptions>;
}
export declare const migrate: Migrate;
//# sourceMappingURL=commander.d.ts.map